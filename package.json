{
  "name": "fwbe-assessment",
  "version": "1.0.0",
  "description": "Flutterwave Backend(Node.js) Engineer Assessment",
  "main": "src/app.js",
  "scripts": {
    "build": "babel src -d lib",
    "build:watch": "babel src -d lib --watch",
    "dev": "nodemon lib/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Lawal Dauda",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.12.10",
    "@babel/core": "^7.12.10",
    "@babel/preset-env": "^7.12.11",
    "nodemon": "^2.0.7"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
